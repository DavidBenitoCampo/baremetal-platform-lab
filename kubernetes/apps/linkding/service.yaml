apiVersion: v1
kind: Service
metadata:
  name: linkding
  namespace: default
spec:
  type: NodePort
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 31000
    protocol: TCP
    name: http
  selector:
    app: linkding
