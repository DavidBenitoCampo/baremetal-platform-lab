# GitHub Actions

CI workflows will be introduced when manifests and configuration are added.

Planned checks:

- YAML formatting
- Kubernetes manifest validation
- Helm validation
- Markdown linting
- Secret scanning
